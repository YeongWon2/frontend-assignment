import React from 'react';
import ActiveWeekLabel from '@/assets/svg/active_week_label.svg';

interface IWeekItemProps {}

function WeekItem(props: IWeekItemProps) {
  return <ActiveWeekLabel />;
}

export default WeekItem;
